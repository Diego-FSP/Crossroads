{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd hoteles && npm install",
    "env": {
      "NODE_VERSION": "18"
    }
  },
  "deploy": {
    "startCommand": "cd hoteles && npm start"
  }
}